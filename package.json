{
  "name": "world-cup-2026",
  "version": "1.0.0",
  "private": true,
  "description": "FIFA World Cup 2026 - Confederation performance dashboard",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
